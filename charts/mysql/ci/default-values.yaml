# CI test values for mysql chart - Deployment mode (default)

image:
  repository: mysql
  tag: "8.0"

secret:
  enabled: true
  envFrom: true
  stringData:
    MYSQL_ROOT_PASSWORD: "ci-test-password"

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    memory: 512Mi
